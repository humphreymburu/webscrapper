    @{
        ViewData["Title"] = "Home Page";
    }

   
    <ul>


        
        $classes = array("tag5", "tag2", "tag9", "tag4", "tag1", "tag6", "tag10", "tag8", "tag3", "tag7" );
        shuffle($tags);
        foreach ($tags as $i => $tag) {
        $class = $classes[$i % count($classes)];
        echo "


        <li><a href='#' class='".$class."'> ".$tag."</a></li>";
        }


        @foreach (var std in ViewBag.WordStrings)
        {

            string[] classes =  {
                "tag5", "tag2", "tag9", "tag4", "tag1", "tag6", "tag10", "tag8", "tag3", "tag7 };

            <li>
                @std
            </li>
        }

    </ul>

    <div class="row justify-content-center text-center align-items-center">
    </div>

